server:
  port: ${PORT:8080}

spring:
  r2dbc:
    url: "r2dbc:h2:file:///[replace this with your db directory];DB_CLOSE_ON_EXIT=FALSE"
  liquibase:
    url: "jdbc:h2:file:[replace this with your db directory];DB_CLOSE_ON_EXIT=FALSE"

app:
  cors:
    allowed-origins: ${APP_CORS_ALLOWED_ORIGINS:https://marekmaciejewski.github.io,http://localhost:5173,http://127.0.0.1:5173}
